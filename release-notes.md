## EntroTect 0.7.14

### 上下文压缩

- 修复 `/compact` 无响应、后续对话报错的问题。手动及自动压缩开始时立即显示状态和停止按钮；超时、取消或摘要不完整时保留原历史并恢复输入。
- 压缩按上下文预算保留近期消息和完整工具调用组，仅在实际减少历史时替换会话内容；主代理和子代理的连续工具回合也会检查压缩阈值。
- 对话中以轻量单行标记显示正在压缩、已压缩及失败等状态；完成标记随会话保存。压缩后先显示用量估算，下一次模型响应后更新实际占用。

### 后台命令

- `bash_output` 在同批其他工具结束后采样，标明采样时间与非增量尾部日志，避免将旧快照误判为当前状态。
- 明确前台默认 120 秒超时与后台无默认超时的语义，后台显式超时仍为 1–600 秒。

### 验证

- 全量测试 521 项通过；Shared、Core 与 Desktop 构建通过，并在 Electron 中检查压缩状态标记。

## EntroTect 0.7.13

### 工具执行可靠性

- PowerShell 工具正确报告原生命令和 cmdlet 的失败；命令链中的较早失败不再被后续成功掩盖。保留显式 `$?` 检查脚本的原有语义。
- Shell 目录按代理隔离、同代理调用按顺序执行；工作目录跨回合及会话重开恢复。默认按 UTF-8 读取中文文件。
- 工具参数 Schema 保留说明、数值范围与整数约束，减少无效调用。
- 前台 Shell 输出设内存上限，大型工具错误在回喂模型前截断并保存日志。

### Ultra 与后台任务

- Ultra 同批子代理调用有失败时，不再放行后续写入或编辑。
- 后台任务按会话隔离；删除会话或退出应用时终止相关进程，结束和超时状态准确显示，输出不再泄漏内部目录标记。
- 删除会话前若后台进程未确认退出，会保留会话并报告错误。

### 验证

- Core 全量测试 490 项通过；Shared、Core 与 Desktop 构建通过。

## EntroTect 0.7.12

### Ultra 调度闭环

- Ultra 首轮同批出现 `task` 与普通工具时，先等待所有子代理返回，再审批和执行后续工具，避免工具调用被误报为未知工具。
- 子代理失败时不会提前执行依赖其结果的写入或编辑操作，并向模型回填明确的未执行原因。
- 补充同批调度成功、失败和模型跳过协作决定的回归测试。

### 验证

- Core 全量测试 476 项通过。
- Shared、Core 与 Desktop 构建通过。

## EntroTect 0.7.11

### 子代理与 Ultra

- Ultra 先作出协作决定，复杂任务主动委派；简单任务可说明原因直接处理。子代理默认使用模型可用的输出容量。
- 主代理等待同轮所有子代理完成。子代理空响应或输出截断时保留工作进度续跑一次；仍无完整报告则明确报错，不再把“无输出”当作成功。
- MiMo 直连支持 low、medium、high 思考档位；Ultra 使用 MiMo 原生 high 并启用子代理编排。

### 编辑安全与界面

- 同文件 read、edit、write 顺序执行；按各代理实际读取内容校验版本，避免并发覆盖。写入先完成临时文件再替换原文件。
- edit 的替换文本按字面处理，修复 JavaScript 美元替换序列导致内容复制的问题；空 old_string 会直接拒绝。
- 右侧详情栏开关常驻标题栏，关闭最后一个标签后保留可展开的空面板；思考内容标题显示为“思考内容”。

### 验证与安装包

- Core 全量测试 474 项通过；Shared、Core、Desktop 构建通过，并在隔离浏览器中验证详情栏交互。
- `EntroTect-Setup-0.7.11.exe` — Windows x64 安装包。
- `SHA256SUMS.txt` — 此版本安装包的 SHA-256 校验和。

## EntroTect 0.7.10

### Plan 与 Goal 协作模式

- 新增 `/plan`、`/goal`、`/compact`、`/help` 内置命令，并与本地 Skills 统一显示在斜杠补全面板中。
- Plan 是可持久化的会话协作模式：先探索、必要时澄清，再交付可直接实施的完整方案。
- Plan 允许读取、搜索、网页调研、只读子代理、测试、构建与静态检查；工具层拒绝文件修改、写入型 Shell、安装、Git 变更、发布及后台进程。
- 完整的 `<proposed_plan>` 回复会渲染成独立“实施计划”卡片，不再作为普通工具或裸文本混在对话流中。
- Goal 可跨回合、会话恢复和上下文压缩保存，支持状态查看、完成、清除与继续推进。

### Harness 与交互改进

- 收紧 Todo 使用条件，Plan 模式不再用 Todo 代替正式方案。
- 优化主代理、Ultra 子代理编排及持续目标提示词，减少不必要限制并强化验证闭环。
- 斜杠面板支持统一键盘游标、Tab/Enter 补全、Esc 关闭、命令用法提示和窄窗口适配。
- Plan/Goal 状态独立显示在输入框上方，不污染对话记录。

### 验证

- Core 全量测试 430 项通过。
- Shared、Core 与 Desktop 完整构建、TypeScript 检查通过。
- 使用 Electron 实际渲染验证计划卡片、斜杠补全、日夜主题及窄窗口布局。

### 资产

- `EntroTect-Setup-0.7.10.exe` — Windows x64 安装包。
- `SHA256SUMS.txt` — 安装包 SHA-256 校验和。
