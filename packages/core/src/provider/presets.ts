// =====================================================================
// Provider presets — expanded builtins with modelsUrl/apiFormat/category/icon
// Single source is shared PROVIDER_PRESETS; re-export for core consumers.
// =====================================================================

export { PROVIDER_PRESETS } from "@entrotect/shared";
